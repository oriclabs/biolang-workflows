# Applied books

These books teach practical analysis with runnable BioLang code. The BioLang
language reference remains in the main BioLang repository because it versions
the language itself rather than an applied workflow.

Each book retains its own build layout. Generated HTML is ignored and should be
published from CI or a release artifact.

