# Day 13: Gene Expression and RNA-seq

| | |
|---|---|
| **Difficulty** | Intermediate |
| **Biology knowledge** | Intermediate (gene expression, RNA-seq workflow, normalization) |
| **Coding knowledge** | Intermediate (tables, pipes, lambda functions, statistics) |
| **Time** | ~3 hours |
| **Prerequisites** | Days 1-12 completed, BioLang installed (see Appendix A) |
| **Data needed** | Generated by `init.bl` (count matrix + gene lengths) |
| **Requirements** | None (offline) |

## What You'll Learn

- What gene expression is and why it matters
- How RNA-seq measures expression by counting reads
- How to work with count matrices (genes x samples)
- Why normalization is essential and how CPM and TPM work
- How to perform differential expression analysis
- What log2 fold change means and how to interpret it
- How to correct for multiple testing
- How to create volcano plots and MA plots

## Files

| File | Description |
|------|-------------|
| `init.bl` | Generates `data/counts.csv` and `data/gene_lengths.csv` |
| `scripts/analysis.bl` | Complete RNA-seq DE pipeline in BioLang |
| `scripts/analysis.py` | Python equivalent (pandas + scipy + statsmodels) |
| `scripts/analysis.R` | R equivalent (DESeq2 + edgeR) |
| `python/requirements.txt` | Python dependencies |
| `r/install.R` | R package installation |
| `compare.md` | Language comparison (BioLang vs Python vs R) |
| `expected/output.txt` | Expected output from `analysis.bl` |

## Quick Start

```bash
cd days/day-13
bl init.bl
bl scripts/analysis.bl
```

## Data Files Generated

- `data/counts.csv` --- 20 genes x 6 samples (3 normal, 3 tumor) count matrix
- `data/gene_lengths.csv` --- gene lengths in base pairs for TPM normalization
