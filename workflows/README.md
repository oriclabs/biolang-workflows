# Complete workflows

This directory contains end-to-end analyses rather than minimal API examples.
Workflows must name required packages, inputs, output locations, random seeds,
and CPU/GPU behavior. Large generated results remain untracked.

